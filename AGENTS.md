# 项目协作规则

- 使用 Python 3.10 或更高版本。
- 所有题目源代码必须放在对应题目的 `src/` 目录。
- 所有 Python 脚本必须支持 argparse 命令行参数。
- 所有文件路径使用 pathlib 和相对路径。
- 不得写死 `D:\code4\qiyin` 或其他本机绝对路径。
- CSV 使用 UTF-8 编码。
- Prompt 模板保存在 YAML 文件中，不写死在 Python 代码中。
- 详细任务元数据保存在 JSON 中。
- 模拟数据必须使用固定随机种子，保证结果可复现。
- 默认流程不得依赖网络或真实商业 API。
- 不要实现平台爬虫。
- 不要把模拟音乐质量分描述为真实听感结果。
- 不要添加数据库、Web 框架或前端项目。
- 公共函数添加类型标注和简要 docstring。
- 程序出错时应输出清晰的错误信息。
- 修改代码后必须运行相关测试和实际 CLI 命令。
- README 中不得声称未实际验证的命令可以运行。
- 不得修改与当前任务无关的文件。
